package com.coiffeur.rdv.service;

import org.springframework.stereotype.Service;

@Service
public class JwtService {

    public String generateToken(String email) {
        return "token"; // temporaire si besoin pour compiler
    }
}
